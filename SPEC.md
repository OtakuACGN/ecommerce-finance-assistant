# 店财通（DianCaiTong）- 技术方案

## 技术栈
- **Electron** + **React** + **TypeScript**
- **xlsx** - Excel/CSV 处理
- **Tailwind CSS** - UI 样式

## 核心功能

### 1. 数据导入
- 支持 CSV、Excel (.xlsx/.xls) 文件
- 文件选择器导入
- 多文件同时导入

### 2. 核心入口
- 共 10 个入口：经营分析、直通车细分、利润测算、销售排行、月度汇总、
  SKU 映射、收款对账、账单对账、快递对账、售后分析
- **经营分析**：订单/账务/商品资料/推广导入、毛利报表、商品资料导出回填
- **月度汇总**：账务月度分析、跨月文件分月归属


## 财务对账功能

### 账单对账
- 支持平台：淘宝、天猫、京东、抖音、抖音电商、快手电商、拼多多
- 自动识别平台（根据文件名）
- 自动解析：账单金额、佣金、平台服务费(净)、补贴
- 生成佣金计提表
- **退款损失还原**：导入退款单，自动计算佣金损失（平台不退佣金）

### 月度汇总
- 按月、按平台汇总：GMV、订单数、佣金、平台服务费(净)、补贴、净收款
- 支持月度环比对比
- 实际收入 = 净收款 - 预估佣金损失

### SKU映射
- 平台品名 → 内部编码 映射表
- 支持价格字段

## 数据模板

各平台账单模板位于 `templates/` 目录，参考 `templates/README.md` 了解详细格式要求。

### 支持的列名关键词

| 数据项 | 支持的列名 |
|--------|-----------|
| 订单金额 | `订单金额`、`商品总额`、`amount`、`total`、`gmv`、`sales` |
| 佣金 | `佣金`、`commission`、`平台服务费`、`扣点` |
| 平台服务费(净) | `技术服务费`、`tech`、`服务费`、`平台费`、`佣金`；服务费/佣金退款或返还会冲减本项，净额允许为负 |
| 补贴/返点 | `补贴`、`subsidy`、`奖励`、`返点`、`rebate`；仅保留真实平台活动补贴，服务费/佣金返还不计入本项 |
| 日期 | `日期`、`账期`、`period`、`date`、`月份` |

## UI 布局

```
┌─────────────────────────────────────────────────┐
│  10 个功能入口（窄桌面自动换行，避免入口被截断）                  │
├─────────────────────────────────────────────────┤
│  当前业务页的导入、参数与操作区                                  │
├─────────────────────────────────────────────────┤
│  经营分析：拖入文件 → 生成报表 → 导出/查看明细              │
├──────────────┬──────────────────────────────────┤
│ 数据来源/参数 │ 汇总卡片、诊断与明细表                            │
│             │                                   │
├──────────────┴──────────────────────────────────┤
│  导出、工作区备份/恢复、异常定位                              │
└─────────────────────────────────────────────────┘
```

## 项目结构

```
diancaitong/
├── electron/
│   ├── main.ts         # Electron 主进程（窗口、IPC）
│   └── preload.ts      # 预加载脚本（contextBridge）
├── src/
│   ├── App.tsx         # 主应用编排（10 个功能入口）
│   ├── main.tsx        # React 入口
│   ├── components/
│   │   ├── DataTable.tsx       # 数据表格
│   │   ├── MonthlySummary.tsx   # 月度汇总
│   │   ├── StatusBar.tsx       # 状态栏
│   │   ├── StatCard.tsx
│   │   └── OpViewNav.tsx
│   ├── services/
│   │   ├── businessLogic.ts   # 业务逻辑（解析账单、对账）
│   │   ├── operatingWorkspace.ts # 工作区备份/恢复
│   │   └── pdd/               # 拼多多解析、合并、报表
│   ├── workers/
│   │   └── operatingReport.worker.ts # 后台报表计算
│   └── utils/
│       ├── desktop.ts   # ElectronAPI 封装
│       └── excel.ts     # Excel/CSV 读写
├── templates/          # 各平台账单模板
│   ├── README.md
│   ├── 淘宝账单模板.csv
│   ├── 天猫账单模板.csv
│   ├── 京东账单模板.csv
│   ├── 抖音账单模板.csv
│   ├── 小红书账单模板.csv
│   ├── 退款单模板.csv
│   └── SKU映射表模板.csv
└── package.json
```

## 构建命令

```bash
npm run dev      # 开发模式
npm run build    # Windows NSIS 安装包
```
